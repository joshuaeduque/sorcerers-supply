interface CartCardProps {
    name: string,
    price: number,
    quantity: number
}

export function CartCard({ name, price, quantity }: CartCardProps) {
    return (
        <div className="flex ">

        </div>
    );
}